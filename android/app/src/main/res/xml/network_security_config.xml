<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS is required everywhere except the loopback addresses used to reach an
  API running on the developer's machine (10.0.2.2 is the host as seen from the
  Android emulator). Production traffic is unaffected by these exemptions.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
